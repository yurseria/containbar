# Theme comparison refraction edit

Mode: built-in image generation, reference-image edit. AI concept, not an actual application screenshot.

Output: `assets/theme-comparison-refraction.png`

References: user-provided split Cobalt/Liquid Glass concept and actual colorful Liquid Glass screenshot.

## Prompt

Edit image 1 as the base for a polished README theme comparison image. Preserve its single continuous Containbar window split exactly vertically: LEFT Cobalt, RIGHT Liquid Glass. Preserve all existing sample names, metadata, port numbers, group counts, icons, title, labels and single shared navigation. This is ONE window split in half, NOT two windows and NOT duplicate lists. Image 2 is ONLY a reference for actual glass curvature, backdrop and optical refraction; do not copy its private container names.

Keep the dark navy Cobalt styling on the left. Replace the weak pastel fog on the right with a visible colorful wallpaper BEHIND the interface, inspired by the rounded vivid blue, magenta, green, orange and purple forms in image 2. Wallpaper should be visible in gaps and outside the UI. Within each right-side glass component, show coherent warped backdrop colors: strong lens-like bending and magnification near the curved boundary, compressed reflected color bands following the contour, softly blurred transmitted background in the center. No opaque colorful fill, no uniformly milky overlay, no excessive white glow. Preserve readable light action icons.

IMPORTANT CURVATURE: match the actual screenshot image 2. Liquid Glass header, navigation, search, group header and individual container cards should have independent pill-shaped right ends, corner radius about HALF each component height, not small rounded rectangles. Separate container cards by small visible gaps, keeping row content aligned seamlessly to the left half. Avoid a large enclosing glass rectangle on the right; the individual glass surfaces should be evident. Maintain sample content from image 1 including frontend/api/db/grafana/prometheus, image versions, up times and ports. Preserve the left-half typography and content hierarchy. High-resolution crisp UI concept, beautifully coherent optical refraction, not an illustration.
